[input]

output = injections_PowerLaw-Gaussian
run-type = injections
injections-number = 100
snr-fw-PSD-path = ~icarogw_pipeline_path/icarogw_pipeline/data/psd

[model]

model-primary = PowerLaw-Gaussian
model-secondary = MassRatio-Gaussian
model-rate = PowerLaw

[snr-options]

[plots]